# Server Configuration
PORT=3000
NODE_ENV=development

# Database
# Create a Neon database and get a connection string from:
#   https://neon.new/
# Do NOT commit your real database credentials.
DATABASE_URL=your-neon-connection-string

# Better Auth
# Generate a secure secret locally using the Better Auth CLI:
#   bun x @better-auth/cli@latest secret
# Do NOT commit your real secret to version control.
BETTER_AUTH_SECRET=replace-with-generated-secret
BETTER_AUTH_URL=http://localhost:3000
